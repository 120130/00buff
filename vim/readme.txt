remember:
	/colors - move to /.vim
	tmux must be ON to work in vim
	Tmuxline powerline - matching colors with console 	
	On Fedora vi is minimal so install vim ( enhanced ) then
	alias vi="vim" - .vimrc file the same for vi and vim so maby set nocompatible
	sudo ln -s `which vim` /usr/local/bin/vi
	sudo ln -s ~/.vim/vimrc ~/.vimrc - elementary .. 
	make dir plugged

